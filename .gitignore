# -----------------------------
# Core OS / Editor / Tooling
# -----------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# Editors / IDEs
.vscode/
.idea/
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# -----------------------------
# Node / React ecosystem
# -----------------------------
node_modules/
bower_components/
jspm_packages/
.npm/
.yarn/
.yarn-cache/
.pnp.*
.pnpm-store/
*.tsbuildinfo

# Build outputs (keep /artifacts tracked if you store releases there)
dist/
build/
out/
.next/
.cache/
coverage/
storybook-static/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env & local overrides
.env
.env.*
!.env.example
.local
*.local

# Lockfiles (optional policy—uncomment to ignore if you don’t want to pin)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# -----------------------------
# Flutter / Dart artifacts
# (in case the pipeline stages write anything here)
# -----------------------------
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.melos_tool/
pubspec.lock
build/

# iOS / Android (if any mobile artifacts pass through)
# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework/
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Flutter/ephemeral/
*.xcworkspace
*.xcuserdatad/
*.xcuserstate

# Android
android/.gradle/
android/local.properties
android/*.iml
android/**/build/
android/captures/

# -----------------------------
# Python (if agent tooling uses it)
# -----------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
.eggs/
.venv/
venv/
pip-wheel-metadata/

# -----------------------------
# Docker / Containers
# -----------------------------
docker-data/
.docker/
*.tar

# -----------------------------
# Archives / Temp
# -----------------------------
*.tar.gz
*.tgz
*.zip
*.7z
*.rar
tmp/
temp/
*.tmp

# -----------------------------
# Artifacts (keep this tracked)
# -----------------------------
# IMPORTANT: If your destination repo stores releases under /artifacts,
# do NOT ignore it. Instead, ignore transient subfolders if needed.
# /artifacts/temp/
# /artifacts/staging/
